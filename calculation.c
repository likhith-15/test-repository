#include<stdio.h>
int main(){
int add a,b,sub;
  printf("Enter the 2 numbers: ");
  scanf("%d",&a);
  scanf("%d",&b);
  add = a+b;
  printf("The sum of the two number is %d",add);
  sub = a-b;
  pritnf("The difference of two numbers is: %d ",sub);
  return 0;

  
}
